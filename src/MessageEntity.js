export default class MessageEntity {
    constructor(userName, text, isInterlocutor) {
        this.userName = userName;
        this.text = text;
        this.isInterlocutor = isInterlocutor;
    }
}